use regex::Regex;
fn main() {
    // let mut v = Vec::new();
    // v.push(1);
    // v.push(2);
    // v.push(3);
    // let mut v = vec![1, 2, 3];
    // let mut v2 = vec!["Sam", "Bob", "Mary"];


    // v.remove(1);
    // println!("{:?}", v);

    // if v2.contains(&"Sam") {
    // println!("found!")}

    let ex = "aam";

    if ex == "[a-z]"{
        println!("matched!");
    }
    
}